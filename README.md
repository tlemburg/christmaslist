install homebrew
`brew install mysql`
`mysql -u root`
-> `create database christmaslist;`

install rbenv
`rbenv install 2.4.4`
`rbenv local 2.4.4`
`gem install bundler`
`bundle install`
`bundle exec rake migrate`
